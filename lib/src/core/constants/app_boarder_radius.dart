import '../app_export.dart';

class AppBoarderRadius{
  static BorderRadius defaultRadius = BorderRadius.circular(6.r);
  static BorderRadius smallRadius = BorderRadius.circular(8.r);
  static BorderRadius mediumRadius = BorderRadius.circular(10.r);
  static BorderRadius largeRadius = BorderRadius.circular(20.r);

}